
language: php
php:
  - '7.1'

conditions: v1

sudo: required

addons:
  chrome: stable

before_install:
  - if [ "$TRAVIS_PULL_REQUEST" != "false" ] ; then php -f commentGithubPullrequest.php; fi

jobs:
  include:
    - stage: prepare
      script: 
        - echo prepare
      after_script:
        - env
        - echo $TRAVIS_TEST_RESULT
